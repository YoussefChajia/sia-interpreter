#ifndef Repl_H
#define Repl_H

#include <iostream>

class Repl {

public:
    static void Start(std::istream& in, std::ostream& out); // Use the fully qualified name "std::istream" and "std::ostream"
};

#endif // Repl_H